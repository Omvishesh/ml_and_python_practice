principal = float(input(" enter the prinicpal : "))
rate = float(input(" enter the rate : "))
time = float(input(" enter the time : "))

simple_intrest = (principal*rate*time)/100

print(f"simple interest = {simple_intrest}")